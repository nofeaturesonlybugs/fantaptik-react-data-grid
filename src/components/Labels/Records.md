`Labels.Records` displays a *Records `Start` to `End` of `Total`* label.

```jsx
<Labels.Records />
```

```jsx
<Labels.Records label="Viewing records" />
```

```jsx
<Labels.Records label="Viewing records" start={5} end={10} total={52} />
```